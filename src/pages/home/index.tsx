import Header from '../../components/header';
import Scheduler from '../../components/scheduler';
import './styles.scss';

const Home = () => {
    return (
        <div>
            <Header />
            <Scheduler />
        </div>
    )
};

export default Home;
